<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <!-- This creates the solid white circle background -->
    <solid android:color="@android:color/white" />

    <!-- Optional: Add a light gray stroke for better visibility, similar to the image -->
    <stroke
        android:width="1dp"
        android:color="#E0E0E0" />
</shape>
